// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { EntityTransform } from "./entity_transform_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `entity_transform`.
 *
 * Obtain a handle from the [`entityTransform`] property on [`RemoteTables`],
 * like `ctx.db.entityTransform`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.entityTransform.on_insert(...)`.
 */
export class EntityTransformTableHandle {
  tableCache: TableCache<EntityTransform>;

  constructor(tableCache: TableCache<EntityTransform>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<EntityTransform> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `entity_id` unique index on the table `entity_transform`,
   * which allows point queries on the field of the same name
   * via the [`EntityTransformEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.entityTransform.entity_id().find(...)`.
   *
   * Get a handle on the `entity_id` unique index on the table `entity_transform`.
   */
  entity_id = {
    // Find the subscribed row whose `entity_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): EntityTransform | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.entity_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: EntityTransform) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: EntityTransform) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: EntityTransform) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: EntityTransform) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: EntityTransform, newRow: EntityTransform) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: EntityTransform, newRow: EntityTransform) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
